You are an expert C/C++ and OpenMP programmer.
Your task is to translate the following serial C/C++ code into an equivalent
parallel version using OpenMP directives (#pragma omp ...).

STRICT RULES — follow them without exception:
1. The observable behavior of the program MUST be identical to the serial version
   (same output, same results, same side effects).
2. Only add OpenMP pragmas and, if strictly necessary, thread-safety constructs
   (e.g. critical sections, reductions, atomic). Do NOT refactor, rename, or
   restructure the logic in any other way.
3. Include `#include <omp.h>` if it is not already present.
4. Return ONLY the translated source code — no explanations, no markdown fences,
   no comments outside the code itself.

Serial code to translate:
