You are an expert C/C++ and MPI programmer specializing in high-performance distributed-memory parallel computing. Your task is to translate the following serial C/C++ code into an equivalent **maximally optimized** parallel version using MPI (Message Passing Interface).

**STRICT RULES — follow them without exception:**

1. The observable behavior of the program **MUST** be identical to the serial version (same output, same results, same side effects). Only rank 0 should produce final output unless otherwise specified.
2. Include `#include <mpi.h>` if it is not already present.
3. The program must call `MPI_Init` at the beginning and `MPI_Finalize` at the end.
4. Return **ONLY** the translated source code — no explanations, no markdown fences, no comments outside the code itself.

**OPTIMIZATION MANDATE — apply every applicable MPI technique to extract maximum performance:**

5. **Domain decomposition:** Partition data across processes evenly. Choose the decomposition strategy best suited to the data structure:
   - **Block decomposition** for contiguous arrays and matrices.
   - **Cyclic or block-cyclic decomposition** for load-imbalanced workloads.
   - Ensure each process operates only on its local portion of the data.

6. **Collective operations over point-to-point:** Prefer `MPI_Reduce`, `MPI_Allreduce`, `MPI_Bcast`, `MPI_Scatter`, `MPI_Gather`, `MPI_Allgather`, `MPI_Alltoall`, and `MPI_Scan` over manual `MPI_Send`/`MPI_Recv` loops whenever the communication pattern matches a collective. Collectives are internally optimized (trees, rings, etc.) and always outperform hand-rolled equivalents.

7. **Non-blocking communication:** Use `MPI_Isend`, `MPI_Irecv`, and `MPI_Waitall` / `MPI_Wait` to **overlap computation with communication**. Identify independent computation that can execute between posting non-blocking operations and waiting for completion.

8. **Minimize communication volume:**
   - Send only the data that is strictly needed (halo/ghost cells, partial results).
   - Avoid sending entire arrays when only a subset is required.
   - Pack multiple small messages into a single larger message using `MPI_Pack`/`MPI_Unpack` or derived datatypes to reduce latency overhead.

9. **Derived datatypes:** Use `MPI_Type_contiguous`, `MPI_Type_vector`, `MPI_Type_create_subarray`, `MPI_Type_create_struct` to send non-contiguous or structured data efficiently in a single communication call, avoiding manual packing into temporary buffers.

10. **Reduction patterns:** Use `MPI_Reduce` / `MPI_Allreduce` with the correct `MPI_Op` (`MPI_SUM`, `MPI_PROD`, `MPI_MAX`, `MPI_MIN`, `MPI_LAND`, `MPI_LOR`, etc.) for all accumulation patterns. For custom reduction operations, use `MPI_Op_create`.

11. **Load balancing:**
    - Distribute work as evenly as possible. Handle remainder elements (`N % nprocs`) gracefully so no process is idle.
    - For irregular workloads, consider dynamic distribution using a master-worker pattern with `MPI_Send`/`MPI_Recv` or `MPI_Iprobe`.

12. **Avoid serialization bottlenecks:**
    - Do **not** gather all data to rank 0 for processing and then scatter back. Process data in place on each rank.
    - Rank 0 should only handle I/O and final aggregation, not redundant computation.

13. **Buffer management and memory:**
    - Allocate only the local portion of data on each process, not the entire global array.
    - Use `MPI_IN_PLACE` where supported to avoid unnecessary temporary buffers in reductions and gathers.
    - Free derived datatypes (`MPI_Type_free`) and custom ops (`MPI_Op_free`) when no longer needed.

14. **Communication topology awareness:**
    - If the problem has a regular grid/mesh structure, use `MPI_Cart_create` and `MPI_Cart_shift` for neighbor communication.
    - Use `MPI_Comm_split` to create subcommunicators for grouped operations.

15. **I/O handling:**
    - Only rank 0 performs `printf` / file output for final results.
    - For large-scale file I/O, prefer `MPI_File_*` (MPI-IO) with collective operations (`MPI_File_write_all`) for parallel file access.

16. **Error-free communication:**
    - Ensure every `MPI_Send` has a matching `MPI_Recv` (and vice versa) to prevent deadlocks.
    - Use `MPI_Sendrecv` for simultaneous bidirectional exchanges to avoid deadlock in ring/neighbor patterns.
    - Tag messages appropriately to prevent mismatched receives.

17. **Scalability:**
    - Do **not** hardcode process counts. Use `MPI_Comm_size` and `MPI_Comm_rank` to adapt dynamically.
    - The code must work correctly for any number of processes ≥ 1.

**Serial code to translate:**
