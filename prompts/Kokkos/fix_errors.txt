You are an expert C++ and Kokkos programmer. The code below was generated as a serial-to-Kokkos translation but fails to compile. Fix ALL compilation errors.

**STRICT RULES:**

1. Observable behavior MUST remain identical to the original serial version.
2. Fix only what is necessary to compile. Do NOT refactor or restructure unrelated logic.
3. Return ONLY the corrected source code — no explanations, no markdown fences.

**DIAGNOSIS CHECKLIST — check and fix these common issues:**

4. **Headers:** Ensure `Kokkos_Core.hpp` is included, plus `Kokkos_ScatterView.hpp`, `Kokkos_DualView.hpp`, `Kokkos_Random.hpp` if used. Include all needed C++ standard headers.

5. **Init/finalize scoping:** `Kokkos::initialize()` before any Kokkos object; `Kokkos::finalize()` after all are destroyed. Wrap Kokkos code in `{}` between init/finalize, or use `ScopeGuard`.

6. **View declarations:** Correct syntax is `Kokkos::View<double*>("label", N)`. Label string is mandatory. Use `*`/`**`/`***` for rank, not fixed-size arrays. Layout and memory space are template parameters, not constructor args.

7. **Lambda capture:** `KOKKOS_LAMBDA` captures by value. Views can be captured (they're handles). Raw pointers/references to host memory cannot — replace with Views. Manual `[=]` lambdas lack `__device__` annotation for CUDA; use `KOKKOS_LAMBDA`.

8. **No host-only code in kernels:** No `std::cout`, `printf`, STL containers, `new/delete`, `malloc/free`, or exceptions inside kernels. Use `Kokkos::printf` for debug output. Standard math functions are fine with `<cmath>`.

9. **Device function annotation:** Any function called from a kernel must be marked `KOKKOS_INLINE_FUNCTION` or `KOKKOS_FUNCTION`.

10. **`parallel_reduce` signatures:** Lambda takes extra reference parameter: `(const int i, double& lsum)`. Don't initialize the reduction variable inside the lambda. Built-in reducer goes as the last argument.

11. **`parallel_scan` signatures:** Lambda is `(const int i, double& update, const bool final)`. Write output only when `final == true`.

12. **Execution policies:** `RangePolicy<>(start, end)` with integers. `MDRangePolicy<Rank<N>>` with brace-enclosed bounds matching rank. `TeamPolicy<>(league_size, Kokkos::AUTO)` when team size is unknown.

13. **Const-correctness:** Functor `operator()` MUST be `const`. Read-only View members should use `Kokkos::View<const double*>`.

14. **deep_copy/mirror:** `deep_copy(dest, src)` requires same dimensions and data type. `create_mirror_view_and_copy(Kokkos::HostSpace(), view)` — space is the first argument. Call `deep_copy` outside parallel regions.

15. **Atomics:** `atomic_add(&view(i), value)` — first arg is a pointer (use `&`). Supported for `int`, `long`, `float`, `double`.

16. **Subviews:** `subview(view, Kokkos::ALL, j)` — `ALL` for retained dims, integer/`pair` for sliced dims. Result rank = number of non-integer arguments.

17. **Template errors:** Mismatched View types in `deep_copy`, wrong reducer types, incorrect space combinations. Use `auto` for mirror views and subviews.

**Current (broken) code:**