You are an expert C++ and Kokkos programmer. Translate the following serial C/C++ code into a maximally optimized parallel Kokkos version.

**STRICT RULES:**

1. Observable behavior MUST be identical to the serial version.
2. Include `#include <Kokkos_Core.hpp>`.
3. Use `Kokkos::ScopeGuard` for init/finalize. No Kokkos objects may exist after finalize.
4. Return ONLY the translated source code — no explanations, no markdown fences.

**OPTIMIZATION RULES — apply every applicable technique:**

5. **Execution patterns:** Use `parallel_for` (no cross-iteration deps), `parallel_reduce` (reductions), `parallel_scan` (prefix sums). Never use raw `for` loops for significant work.

6. **Execution policies:** Use `RangePolicy` for 1D, `MDRangePolicy<Rank<N>>` for nested multi-dimensional loops, `TeamPolicy` with `TeamThreadRange`/`ThreadVectorRange` for hierarchical parallelism. Use `Kokkos::AUTO` for team/vector sizes unless profiling justifies specific values.

7. **Views and memory:** Replace all raw arrays/malloc/new with `Kokkos::View`. Use default memory space and layout (adapts to execution space). Use `LayoutRight` for CPU-friendly, `LayoutLeft` for GPU-friendly access only when overriding is necessary.

8. **Host-device transfers:** Use `create_mirror_view` + `deep_copy`. Use `create_mirror_view_and_copy` for convenience. Minimize transfers — compute on device, copy results back only when needed.

9. **Reductions:** Use `parallel_reduce` with built-in reducers (`Sum`, `Min`, `Max`, `MinLoc`, `MaxLoc`, etc.). Never replace reductions with atomics.

10. **Atomics and scatter:** Use atomics only when scatter updates are unavoidable. Prefer `Kokkos::ScatterView` for scatter-add patterns.

11. **Memory access:** Ensure coalesced access in the innermost parallel dimension. Avoid strided patterns. Use `Kokkos::Subview` instead of copying subsets.

12. **Scratch memory:** Use team scratch (`set_scratch_size`) for data reused within a team. Level 0 for small/fast, level 1 for larger needs.

13. **Kernel fusion:** Minimize kernel launches. Fuse consecutive kernels when dependencies allow. Use `Kokkos::fence()` only when strictly necessary.

14. **Lambdas and functors:** Prefer `KOKKOS_LAMBDA` for short kernels. Use functors with `KOKKOS_INLINE_FUNCTION` for complex/reusable logic. Mark all device-callable functions with `KOKKOS_INLINE_FUNCTION`.

15. **Portability:** No raw CUDA/HIP/OpenMP intrinsics. No STL containers, `std::cout`, `printf`, `malloc`, or `new` inside kernels (use `Kokkos::printf` if needed). All kernel data must reside in Views or be captured by value.

16. **I/O and init:** File I/O and `std::cout` on host only. Initialize Views via mirror views + deep_copy, or via `parallel_for` on device.

17. **No hardcoding:** Use default execution/memory spaces. Do not hardcode team sizes or vector lengths.

**Serial code to translate:**

[PASTE YOUR CODE HERE]